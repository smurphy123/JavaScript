const kelvin = 0; // constant value for kelvin value
const celsius = kelvin - 273; // constant value for celcius value

// Fahrenheit conversion
const fahrenheit = Math.floor(celsius * (9/5) + 32);
// Prin fahrenheit temperature
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);
